import { Stack } from "@mui/material";

export default function Blue() {
  return (
    <Stack sx={{flex: 1, backgroundColor: "blue", alignItems: "center", justifyContent: "center"}}>
      Blue
    </Stack>
  )
}