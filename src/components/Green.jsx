import { Stack } from "@mui/material";

export default function Green() {
  return (
    <Stack sx={{flex: 1, backgroundColor: "green", alignItems: "center", justifyContent: "center"}}>
      Green
    </Stack>
  )
}