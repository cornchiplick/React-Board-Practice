import { Stack } from "@mui/material";

export default function Red() {
  return (
    <Stack sx={{flex: 1, backgroundColor: "red", alignItems: "center", justifyContent: "center"}}>
      Red
    </Stack>
  )
}